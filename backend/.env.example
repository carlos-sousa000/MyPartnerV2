# Local/cloud backend configuration
GROQ_API_KEY=
DEFAULT_COMPANY_ID=1

# Firebase Admin service account JSON path (local) or use platform secret storage
FIREBASE_CRED_JSON=backend/firebase-service-account.json

# Set true only after Firestore and Firebase Auth are configured
USE_FIRESTORE=false
REQUIRE_AUTH=false
