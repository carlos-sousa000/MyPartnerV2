rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The current backend uses Firebase Admin, so client-side access stays closed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}